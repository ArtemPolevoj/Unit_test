    Для тестов метода getRandomInteger из класса HwList были выбраны и реализованы
юнит-тесты.
Было проверено:
   1. метод возвращает именно класс ArrayList.
   2. метод возвращает ArrayList заданной длины.
   3. метод возвращает ArrayList в указанном диапазоне min, max.

    Для тестов метода getAverage из класса Homework был выбран и реализован
интеграционный тест. В параметры метода был передан реальный результат
метода getRandomInteger из класса HwList. Далее был вычислен проверочный результат
другим способом с применением стандартных методов и сравнён результатом работы
getAverage.

    Для теста метода equalsAverage из класса Homework был выбран и реализован
интеграционный тест с применением тестовых заглушек типа Mock. Заглушки позволили
реализовать поведение метода getRandomInteger из класса HwList.